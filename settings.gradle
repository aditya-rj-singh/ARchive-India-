pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
    }
}
include ':unityLibrary'
project(':unityLibrary').projectDir=new File('C:\\Users\\Admin\\AndroidStudioProjects\\AR-App\\unityLibrary')
dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()

        flatDir{
            dirs "${project(':unityLibrary').projectDir}/libs"
        }

    }
}
rootProject.name = "HistoryandCulture"

include ':app'



include ':unityLibrary:xrmanifest.androidlib'
project(':unityLibrary:xrmanifest.androidlib').projectDir = new File(rootDir, 'unityLibrary/xrmanifest.androidlib')